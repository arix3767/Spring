server:
  servlet:
    context-path: /student-classes
spring:
  datasource:
    url: jdbc:h2:mem:student-classes
    username: user
    password: 1234
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect